#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

/**
 * main - main function
 * Return: 0 if the program finish whitout errors
 */
int	main(void)
{
	int	A;

	A = '0';
	while (A <= '9')
	{
		putchar(n);
		if (A != '9')
		{
			putchar(',');
			putchar(' ');
		}
		A++;
	}
	putchar('\n');
	return (0);
}
