# include "main.h"

/**
 * _isupper - check if c is upper
 *
 * @c: input for alphabet
 *
 * Return: Returns 1 if c is uppercase, Returns 0 if not.
 */

int _isupper(int c)
{
	return ((c >= 'A' && c <= 'Z'));
}
